{
  "name": "kiosque-splitter",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "install:all": "npm i --prefix server && npm i --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
